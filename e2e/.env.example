# =============================================================================
# AWS Device Farm Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Credentials (Required)
# -----------------------------------------------------------------------------
# Get these from AWS IAM Console:
# 1. Go to IAM > Users > Your User > Security credentials
# 2. Create access key > Command Line Interface (CLI)

AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# -----------------------------------------------------------------------------
# AWS Region (Optional)
# -----------------------------------------------------------------------------
# AWS Device Farm is only available in us-west-2
# Default: us-west-2

AWS_REGION=us-west-2

# -----------------------------------------------------------------------------
# Device Farm Project ARN (Required)
# -----------------------------------------------------------------------------
# Get this from AWS Device Farm Console:
# 1. Go to Device Farm > Mobile device testing > Projects
# 2. Click on your project
# 3. Copy the Project ARN from the details
#
# Format: arn:aws:devicefarm:us-west-2:ACCOUNT_ID:project:PROJECT_ID

AWS_DEVICE_FARM_PROJECT_ARN=arn:aws:devicefarm:us-west-2:123456789012:project:abcd1234-5678-90ab-cdef-1234567890ab

# -----------------------------------------------------------------------------
# Device Pool ARNs (Optional)
# -----------------------------------------------------------------------------
# If not specified, the script will:
# 1. Look for a private pool named "Android-Test-Devices" or "iOS-Test-Devices"
# 2. Fall back to AWS's curated "Top Devices" pool
#
# Get these from AWS Device Farm Console:
# 1. Go to Device Farm > Your Project > Device pools
# 2. Click on each pool to see its ARN
#
# Or via AWS CLI:
#   aws devicefarm list-device-pools \
#     --arn $AWS_DEVICE_FARM_PROJECT_ARN \
#     --type PRIVATE \
#     --region us-west-2 \
#     --query 'devicePools[*].[name,arn]' \
#     --output table

AWS_DEVICE_POOL_ARN_ANDROID=arn:aws:devicefarm:us-west-2:123456789012:devicepool:abcd1234/android-pool-id
AWS_DEVICE_POOL_ARN_IOS=arn:aws:devicefarm:us-west-2:123456789012:devicepool:abcd1234/ios-pool-id

# =============================================================================
# Usage Examples
# =============================================================================
#
# Run Android tests on Device Farm:
#   yarn test:aws --platform android
#
# Run iOS tests on Device Farm:
#   yarn test:aws --platform ios
#
# Run with custom app path:
#   yarn test:aws --platform android --app /path/to/app.apk
#   yarn test:aws --platform ios --app /path/to/app.ipa
#
# Default app paths (if --app not specified):
#   Android: ../android/app/build/outputs/apk/release/app-release.apk
#   iOS: ../ios/build/LOCAI.ipa
#
# =============================================================================
